# Excel Add-in Backend Environment Configuration
# Copy this file to .env and configure the values according to your environment

# Application Configuration
DEBUG=false
HOST=0.0.0.0
PORT=5000

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,https://localhost:3000

# NiFi Endpoint Configuration
NIFI_ENDPOINT=https://server-vs81t.intranet.local:8443/nifi/api/excel-addin-upload

# SSL/TLS Configuration for NiFi Connection
# Set to 'false' to disable SSL verification (not recommended for production)
NIFI_VERIFY_SSL=true

# Certificate Paths (relative to backend/certificates/ directory)
# Uncomment and configure these if you have specific certificate files

# CA certificate for verifying the NiFi server's SSL certificate
# If not specified, the system will look for 'nifi-ca-cert.pem' in the certificates directory
# NIFI_CA_CERT_PATH=nifi-ca-cert.pem

# Client certificate and private key for mutual TLS authentication (optional)
# Only needed if NiFi requires client certificate authentication
# NIFI_CLIENT_CERT_PATH=nifi-client-cert.pem
# NIFI_CLIENT_KEY_PATH=nifi-client-key.pem

# Alternative: If client cert and key are in the same file
# NIFI_CLIENT_CERT_PATH=nifi-client-combined.pem

# Example configurations for different scenarios:

# Scenario 1: Self-signed NiFi certificate
# NIFI_CA_CERT_PATH=nifi-self-signed.pem
# NIFI_VERIFY_SSL=true

# Scenario 2: Corporate CA-signed certificate
# NIFI_CA_CERT_PATH=corporate-ca-bundle.pem
# NIFI_VERIFY_SSL=true

# Scenario 3: Mutual TLS with separate cert and key files
# NIFI_CA_CERT_PATH=nifi-ca-cert.pem
# NIFI_CLIENT_CERT_PATH=client.crt
# NIFI_CLIENT_KEY_PATH=client.key
# NIFI_VERIFY_SSL=true

# Scenario 4: Disable SSL verification (development only)
# NIFI_VERIFY_SSL=false